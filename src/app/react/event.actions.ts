import { createAction } from '@ngrx/store';

export const ADD_EVENT = createAction('[Add Event]');
export const DELETE_EVENT = createAction('[Delete Event]');
export const RESET_EVENT = createAction('[Reset Event]');
